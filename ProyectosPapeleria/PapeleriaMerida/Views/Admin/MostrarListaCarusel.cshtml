@using System.Data;
@model DataTable
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">Datos guardados</div>
            <div class="panel-body">
                <div class="col-md-12">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">Código</th>
                                <th scope="col">Titulo1/Superior</th>
                                <th scope="col">Titulo2/Posterior</th>
                                <th scope="col">Imagen</th>
                                <th scope="col">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (DataRow item in Model.Rows)
                            {
                                <tr id="Fila_@item["idCarusel"]">
                                    <th scope="row">@item["idCarusel"]</th>
                                    <td>@item["titulo1"]</td>
                                    <td>@item["titulo2"]</td>
                                    <td><img style="width: 150px; height: 100px" src="~/images/Carusel/@item["urlCarusel"]" /></td>
                                    <td>
                                        <a type="button" rel="tooltip" title="Editar" class="btn btn-success btn-simple btn-xs" href="@Url.Action("ModificarCarusel", "Admin", new { id =item["idCarusel"]  })">
                                            <i class="fa fa-edit"></i>
                                        </a>
                                        <a type="button" rel="tooltip" title="Eliminar" class="btn btn-danger btn-simple btn-xs" href="@Url.Action("EliminarCarusel", "Admin", new { id =item["idCarusel"]  })">
                                            <i class="fa fa-times"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div><!-- /.panel-->
    </div><!-- /.panel-->
</div><!-- /.col-->
